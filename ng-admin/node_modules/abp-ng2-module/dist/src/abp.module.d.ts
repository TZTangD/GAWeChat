import { Http, XHRBackend, RequestOptions } from '@angular/http';
import { AbpHttpConfiguration } from './abpHttp';
export declare function abpHttpFactory(xhrBackend: XHRBackend, requestOptions: RequestOptions, configuration: AbpHttpConfiguration): Http;
export declare let ABP_HTTP_PROVIDER: {
    provide: typeof Http;
    useFactory: (xhrBackend: XHRBackend, requestOptions: RequestOptions, configuration: AbpHttpConfiguration) => Http;
    deps: (typeof AbpHttpConfiguration | typeof XHRBackend | typeof RequestOptions)[];
};
export declare class AbpModule {
}
