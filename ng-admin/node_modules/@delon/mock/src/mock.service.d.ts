import { OnDestroy } from '@angular/core';
import { MockOptions } from '../mock.options';
import { MockRule } from './interface';
export declare class MockService implements OnDestroy {
    private config;
    private cached;
    constructor(config: MockOptions);
    private applyMock();
    private realApplyMock();
    private genRule(key, callback);
    private outputError(error);
    getRule(method: string, url: string): MockRule;
    clearCache(): void;
    ngOnDestroy(): void;
}
