import { ElementRef, TemplateRef, OnInit, Renderer2 } from '@angular/core';
import { Router } from '@angular/router';
import { MenuService, AlainI18NService } from '@delon/theme';
export declare class ProHeaderComponent implements OnInit {
    private route;
    private menuSrv;
    private i18nSrv;
    private el;
    private renderer;
    title: string;
    /**
     * 自动生成导航，以当前路由从主菜单中定位
     */
    autoBreadcrumb: any;
    private _autoBreadcrumb;
    paths: any[];
    breadcrumb: TemplateRef<any>;
    logo: TemplateRef<any>;
    action: TemplateRef<any>;
    content: TemplateRef<any>;
    extra: TemplateRef<any>;
    tab: TemplateRef<any>;
    constructor(route: Router, menuSrv: MenuService, i18nSrv: AlainI18NService, el: ElementRef, renderer: Renderer2);
    private genBreadcrumb();
    ngOnInit(): void;
}
