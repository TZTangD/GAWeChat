import { ElementRef, OnChanges, SimpleChanges, Renderer2, SimpleChange } from '@angular/core';
export declare class StandardFormRowComponent implements OnChanges {
    private el;
    private renderer;
    title: string;
    /** 是否整行 */
    block: any;
    private _block;
    /** 是否最后一行 */
    last: any;
    private _last;
    /** 是否网格布局 */
    grid: any;
    private _grid;
    constructor(el: ElementRef, renderer: Renderer2);
    _classMap: string[];
    setClass(): void;
    ngOnChanges(changes: {
        [P in keyof this]?: SimpleChange;
    } & SimpleChanges): void;
}
