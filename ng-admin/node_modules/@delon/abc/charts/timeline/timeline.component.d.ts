import { ElementRef, OnDestroy, OnChanges, SimpleChanges, OnInit, TemplateRef, SimpleChange, ChangeDetectorRef } from '@angular/core';
export declare class TimelineComponent implements OnDestroy, OnChanges, OnInit {
    private cd;
    _title: string;
    _titleTpl: TemplateRef<any>;
    title: string | TemplateRef<any>;
    data: Array<{
        x: Date;
        y1: number;
        y2: number;
        [key: string]: any;
    }>;
    titleMap: {
        y1: string;
        y2: string;
    };
    colorMap: {
        y1: string;
        y2: string;
    };
    height: any;
    private _height;
    padding: number[];
    borderWidth: any;
    private _borderWidth;
    node: ElementRef;
    sliderNode: ElementRef;
    chart: any;
    initFlag: boolean;
    slider: any;
    constructor(cd: ChangeDetectorRef);
    ngOnInit(): void;
    install(): void;
    uninstall(): void;
    ngOnChanges(changes: {
        [P in keyof this]?: SimpleChange;
    } & SimpleChanges): void;
    ngOnDestroy(): void;
}
