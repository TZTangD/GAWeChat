import { ElementRef, OnDestroy, OnChanges, SimpleChanges, TemplateRef, OnInit, Renderer2, SimpleChange, ChangeDetectorRef } from '@angular/core';
export declare class WaterWaveComponent implements OnDestroy, OnChanges, OnInit {
    private el;
    private renderer;
    private cd;
    _title: string;
    _titleTpl: TemplateRef<any>;
    title: string | TemplateRef<any>;
    color: string;
    height: any;
    private _height;
    percent: any;
    private _percent;
    node: ElementRef;
    initFlag: boolean;
    timer: any;
    constructor(el: ElementRef, renderer: Renderer2, cd: ChangeDetectorRef);
    renderChart(): void;
    uninstall(): void;
    private updateRadio(radio);
    ngOnInit(): void;
    ngOnChanges(changes: {
        [P in keyof this]?: SimpleChange;
    } & SimpleChanges): void;
    ngOnDestroy(): void;
    private autoHideXLabels;
    private scroll$;
    private installResizeEvent();
    private uninstallResizeEvent();
    resize(): void;
}
