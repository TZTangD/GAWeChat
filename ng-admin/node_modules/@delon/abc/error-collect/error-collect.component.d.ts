import { OnInit, OnDestroy, ElementRef, Renderer2, ChangeDetectorRef } from '@angular/core';
/**
 * 错误消息采集器
 * PS：虽然此法并不好看，但对响应式表单&模板表单有很好的效果。
 */
export declare class ErrorCollectComponent implements OnInit, OnDestroy {
    private el;
    private renderer;
    private cd;
    private doc;
    $time: any;
    formEl: HTMLFormElement;
    tick: any;
    private _tick;
    _hiden: boolean;
    count: number;
    constructor(el: ElementRef, renderer: Renderer2, cd: ChangeDetectorRef, doc: any);
    private update();
    _click(): boolean;
    private install();
    private uninstall();
    private findParent(el, selector);
    ngOnInit(): void;
    ngOnDestroy(): void;
}
