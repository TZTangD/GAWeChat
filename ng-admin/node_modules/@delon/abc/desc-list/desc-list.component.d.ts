import { ElementRef, Renderer2, OnChanges, SimpleChanges, OnInit, TemplateRef, QueryList, SimpleChange } from '@angular/core';
import { DescListItemComponent } from './desc-list-item.component';
export declare class DescListComponent implements OnChanges, OnInit {
    private el;
    private renderer;
    _title: string;
    _titleTpl: TemplateRef<any>;
    title: string | TemplateRef<any>;
    size: 'small' | 'large';
    /** 列表项间距，单位为 `px` */
    gutter: any;
    private _gutter;
    layout: 'horizontal' | 'vertical';
    _xs: number;
    _sm: number;
    _md: number;
    /** 指定信息最多分几列展示，最终一行几列由 col 配置结合响应式规则决定 */
    col: any;
    private _col;
    _classMap: string[];
    setClass(): void;
    setResponsive(): void;
    _items: QueryList<DescListItemComponent>;
    constructor(el: ElementRef, renderer: Renderer2);
    ngOnInit(): void;
    ngOnChanges(changes: {
        [P in keyof this]?: SimpleChange;
    } & SimpleChanges): void;
}
