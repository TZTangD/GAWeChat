import { _HttpClient } from '@delon/theme';
import { XlsxExportOptions, XlsxConfig } from './interface';
import { LazyService } from '../utils/lazy.service';
export declare class XlsxService {
    private config;
    private _http;
    private lazy;
    constructor(config: XlsxConfig, _http: _HttpClient, lazy: LazyService);
    private init();
    private read(wb);
    /** 导入Excel并输出JSON，支持 `<input type="file">`、URL 形式 */
    import(fileOrUrl: File | string): Promise<{
        [key: string]: any[][];
    }>;
    /** 导出 */
    export(options: XlsxExportOptions): Promise<void>;
}
