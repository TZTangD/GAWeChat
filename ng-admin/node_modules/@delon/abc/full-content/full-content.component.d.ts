import { ElementRef, Renderer2, ChangeDetectorRef, EventEmitter, OnChanges, SimpleChanges, SimpleChange, OnInit, OnDestroy, AfterViewInit } from '@angular/core';
import { FullContentService } from './full-content.service';
import { Router } from '@angular/router';
export declare class FullContentComponent implements OnInit, AfterViewInit, OnChanges, OnDestroy {
    private el;
    private render;
    private router;
    private cd;
    private srv;
    private doc;
    private bodyEl;
    private inited;
    private srv$;
    private route$;
    id: string;
    _height: number;
    fullscreen: any;
    private _fullscreen;
    hideTitle: any;
    private _hideTitle;
    padding: any;
    private _padding;
    fullscreenChange: EventEmitter<boolean>;
    constructor(el: ElementRef, render: Renderer2, router: Router, cd: ChangeDetectorRef, srv: FullContentService, doc: Document);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    private updateFsCls();
    private update();
    private updateHeight();
    toggle(): void;
    ngOnChanges(changes: {
        [P in keyof this]?: SimpleChange;
    } & SimpleChanges): void;
    ngOnDestroy(): void;
    private scroll$;
    private installResizeEvent();
    private uninstallResizeEvent();
}
