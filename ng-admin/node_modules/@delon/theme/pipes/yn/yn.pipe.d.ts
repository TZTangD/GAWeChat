import { PipeTransform } from '@angular/core';
/**
 * 将boolean类型徽章化
 *
 * @example
 * ```html
 * <td [innerHTML]="enabled | yn"></td>
 * ```
 */
export declare class YNPipe implements PipeTransform {
    transform(value: any, yes: string, no: string): string;
}
