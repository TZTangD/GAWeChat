import { Title } from '@angular/platform-browser';
import { MenuService } from '../menu/menu.service';
import { AlainI18NService } from '../i18n/i18n';
/**
 * 设置标题（用法见 AppComponent）
 * 标题获取的优先级为：route（支持i18n） > html
 *  + 路由：配置标准的方法见 `./routes/routes.ts` 路由配置节点中 `data` 属性，允许设置：`translate` 或 `title` 两个参数，前者 i18n.
 *  + html：若不指定则从 `content__title` 中获取 `h1` 内容
 */
export declare class TitleService {
    private title;
    private menuSrv;
    private translatorSrv;
    private doc;
    constructor(title: Title, menuSrv: MenuService, translatorSrv: AlainI18NService, doc: any);
    private _prefix;
    private _suffix;
    private _separator;
    private _reverse;
    /** 设置分隔符 */
    separator: string;
    /** 设置前缀 */
    prefix: string;
    /** 设置后缀 */
    suffix: string;
    /** 设置是否反转 */
    reverse: boolean;
    private getByElement();
    /**
     * 设置标题
     * 若不指定则从 `content__title` 中获取 `h1` 内容
     */
    setTitle(title?: string | string[]): void;
    /**
     * 根据URL地址从 `MenuService` 中获取对应的标题
     */
    setTitleByUrl(url: string): void;
}
