import { InjectionToken } from '@angular/core';
export interface AlainThemeOptions {
    [key: string]: any;
    /**
     * 后端网址，建议配合 `environment` 一起使用
     */
    SERVER_URL: string;
}
export declare const ALAIN_THEME_OPTIONS: InjectionToken<AlainThemeOptions>;
