import { NzMessageService } from 'ng-zorro-antd';
import { NzClipboardConfig } from './config';
export declare class NzClipboardService {
    private document;
    private messageSrv;
    private tempTextArea;
    private config;
    constructor(document: any, cog: NzClipboardConfig, messageSrv: NzMessageService);
    readonly isSupported: boolean;
    isTargetValid(element: HTMLInputElement | HTMLTextAreaElement): boolean;
    /**
     * copyFromInputElement
     */
    copyFromInputElement(targetElm: HTMLInputElement | HTMLTextAreaElement, success?: string, error?: string): boolean;
    /**
     * Creates a fake textarea element, sets its value from `text` property,
     * and makes a selection on it.
     */
    copyFromContent(content: string, success?: string, error?: string): boolean;
    destroy(): void;
    private selectTarget(inputElement);
    private copyText();
    private clearSelection(inputElement);
    private createTempTextArea(doc);
    private handleMessage(status, focus?);
}
