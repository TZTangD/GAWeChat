import { OnDestroy, EventEmitter } from '@angular/core';
import { NzClipboardService } from './service';
import { NzInputComponent } from 'ng-zorro-antd';
export interface NzClipboardNotify {
    /**
     * 是否复制成功
     */
    status: boolean;
    /**
     * 复制内容
     */
    content?: string;
}
export declare class NzClipboardDirective implements OnDestroy {
    private clipboardSrv;
    content: string;
    target: HTMLInputElement | NzInputComponent;
    successText: string;
    errorText: string;
    success: EventEmitter<NzClipboardNotify>;
    error: EventEmitter<NzClipboardNotify>;
    constructor(clipboardSrv: NzClipboardService);
    _click(): void;
    private handleResult(succeeded, copiedContent);
    ngOnDestroy(): void;
}
