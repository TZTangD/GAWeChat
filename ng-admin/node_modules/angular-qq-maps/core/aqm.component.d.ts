import { ElementRef, OnDestroy, EventEmitter, NgZone, OnChanges, SimpleChanges } from '@angular/core';
import { LoaderService } from './loader.service';
import { AqmConfig } from './aqm.config';
export declare class AqmComponent implements OnChanges, OnDestroy {
    private el;
    private COG;
    private loader;
    private zone;
    options: any;
    ready: EventEmitter<any>;
    private map;
    constructor(el: ElementRef, COG: AqmConfig, loader: LoaderService, zone: NgZone);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private _initMap();
    private _updateOptions();
    private destroy();
    ngOnDestroy(): void;
}
