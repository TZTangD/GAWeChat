import { ElementRef, OnDestroy, EventEmitter, NgZone, OnChanges, SimpleChanges } from '@angular/core';
import { LoaderService } from './loader.service';
import { AbmConfig } from './abm.config';
export declare class AbmPanoramaComponent implements OnChanges, OnDestroy {
    private el;
    private COG;
    private loader;
    private zone;
    options: any;
    ready: EventEmitter<any>;
    private map;
    constructor(el: ElementRef, COG: AbmConfig, loader: LoaderService, zone: NgZone);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private _initMap();
    private _updateOptions();
    private destroy();
    ngOnDestroy(): void;
}
