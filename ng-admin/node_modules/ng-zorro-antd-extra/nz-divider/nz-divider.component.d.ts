import { TemplateRef, ElementRef, Renderer2, OnChanges, SimpleChanges, OnInit } from '@angular/core';
export declare class NzDividerComponent implements OnChanges, OnInit {
    private el;
    private renderer;
    title: boolean;
    _title: string;
    _titleTpl: TemplateRef<any>;
    nzTitle: string | TemplateRef<any>;
    nzType: 'horizontal' | 'vertical';
    nzDashed: boolean;
    _classMap: string[];
    setClass(): void;
    constructor(el: ElementRef, renderer: Renderer2);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
}
