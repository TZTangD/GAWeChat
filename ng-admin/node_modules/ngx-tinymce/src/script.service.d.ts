import { Subject } from 'rxjs/Subject';
export declare class ScriptService {
    private doc;
    private loaded;
    private list;
    private emitter;
    constructor(doc: any);
    getChangeEmitter(): Subject<boolean>;
    load(path: string): this;
    loadScript(path: string): Promise<any>;
}
